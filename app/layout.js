export const metadata = {
  title: "PlayHub Control",
  description: "Controle financeiro PlayHub",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
